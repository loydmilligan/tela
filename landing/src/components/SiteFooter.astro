---
const year = 2026;
---
<footer class="site-footer">
  <div class="inner">
    <div class="brand-col">
      <span class="brand">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" aria-hidden="true">
          <rect x="0.5" y="0.5" width="23" height="23" rx="6" fill="var(--cta-bg)" />
          <g stroke="var(--brand-fg)" stroke-width="2" opacity="0.92">
            <path d="M8 3.5V20.5" /><path d="M16 3.5V20.5" /><path d="M3.5 8H20.5" /><path d="M3.5 16H20.5" />
          </g>
        </svg>
        tela
      </span>
      <p class="tag">tela — the agent-native team wiki your AI reasons over.</p>
    </div>
    <nav class="links" aria-label="Footer">
      <a href="https://github.com/zcag/tela" target="_blank" rel="noopener">GitHub</a>
      <a href="/mcp">MCP / connector</a>
      <a href="https://github.com/zcag/tela#readme" target="_blank" rel="noopener">Docs</a>
      <a href="https://tela.cagdas.io" target="_blank" rel="noopener">Live instance</a>
      <a href="/privacy">Privacy</a>
      <a href="https://github.com/zcag/tela/blob/main/LICENSE" target="_blank" rel="noopener">License</a>
    </nav>
  </div>
  <div class="base">
    <span>© {year} tela — hosted, or run it yourself.</span>
    <span class="ms">tela — Latin for the woven cloth. A grid you build on.</span>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--border);
    background: var(--surface-sunk);
  }
  .inner {
    max-width: var(--col-max);
    margin-inline: auto;
    padding: 3rem var(--space-gutter) 2rem;
    display: grid;
    gap: 2rem;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.05rem;
    color: var(--fg);
  }
  .tag { margin-top: 0.7rem; max-width: 30rem; color: var(--fg-faint); font-size: var(--text-sm); }
  .links { display: flex; flex-wrap: wrap; gap: 1.25rem; }
  .links a { color: var(--fg-soft); font-size: var(--text-sm); }
  .links a:hover { color: var(--fg); }
  .base {
    max-width: var(--col-max);
    margin-inline: auto;
    padding: 1.1rem var(--space-gutter);
    border-top: 1px solid var(--border);
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.5rem;
    color: var(--fg-faint);
    font-size: var(--text-xs);
  }
  .ms { font-family: var(--font-mono); }
  @media (min-width: 760px) {
    .inner { grid-template-columns: 1.3fr 1fr; align-items: start; }
  }
</style>
